<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Custom WYSIWYG Editor</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
    }
    #toolbar {
      margin-bottom: 10px;
    }
    #toolbar button {
      margin-right: 5px;
      padding: 5px 10px;
      border: 1px solid #ccc;
      background: #f8f8f8;
      cursor: pointer;
      border-radius: 4px;
    }
    #toolbar button:hover {
      background: #eee;
    }
    #editor {
      border: 1px solid #ccc;
      min-height: 200px;
      padding: 10px;
      border-radius: 6px;
    }
    iframe {
      max-width: 100%;
      border-radius: 6px;
      margin: 8px 0;
    }
  </style>
</head>
<body>
  <h2>My Custom WYSIWYG Editor</h2>

  <!-- Toolbar -->
  <div id="toolbar">
    <button onclick="format('bold')"><b>B</b></button>
    <button onclick="format('italic')"><i>I</i></button>
    <button onclick="format('underline')"><u>U</u></button>
    <button onclick="insertLink()">🔗 Link</button>
    <button onclick="insertIframe()">📺 YouTube</button>
  </div>

  <!-- Editable Area -->
  <div id="editor" contenteditable="true">
    Type something here...
  </div>

  <script>
    // Basic formatting
    function format(command) {
      document.execCommand(command, false, null);
    }

    // Insert hyperlink
    function insertLink() {
      let url = prompt("Enter URL:");
      if (url) {
        document.execCommand("createLink", false, url);
      }
    }

    // Insert YouTube iframe
    function insertIframe() {
      let url = prompt("Enter YouTube URL:");
      if (!url) return;

      let embedUrl = url;

      // Detect YouTube and convert to embed link
      const ytMatch = url.match(/(?:youtube\.com\/watch\?v=|youtu\.be\/)([a-zA-Z0-9_-]+)/);
      if (ytMatch && ytMatch[1]) {
        embedUrl = `https://www.youtube.com/embed/${ytMatch[1]}`;
      } else {
        alert("Currently only YouTube URLs are supported!");
        return;
      }

      const iframeHTML = `
        <iframe width="560" height="315"
          src="${embedUrl}"
          frameborder="0"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen>
        </iframe>`;

      document.execCommand("insertHTML", false, iframeHTML);
    }
  </script>
</body>
</html>
