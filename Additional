function setValueByPath(obj, path, value) {
    if (!path) return; // Ensure path is valid

    const keys = path.split('.'); // Split the path into keys
    let current = obj;

    for (let i = 0; i < keys.length - 1; i++) {
        if (!current[keys[i]] || typeof current[keys[i]] !== 'object') {
            current[keys[i]] = {}; // Create missing object
        }
        current = current[keys[i]]; // Move deeper
    }

    // Set the final value (always set/overwrite)
    current[keys[keys.length - 1]] = value;
}

// Example JSON object
let json_template_to_be_filled = {
    "tabsDetails": {
        "Support": {
            "someOtherKey": "Existing Value"
        }
    }
};

// Example path to set value
let jsonPath = "tabsDetails.Support.tabContent.productArea";
let productArea = "Product Value";

setValueByPath(json_template_to_be_filled, jsonPath, productArea);

console.log(JSON.stringify(json_template_to_be_filled, null, 2));
