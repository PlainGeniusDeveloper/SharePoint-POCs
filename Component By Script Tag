function bindDataToTemplate(template, data) {
    // Loop over each key in the data object
    for (let key in data) {
        // Replace {{key}} in the template with the actual value
        template = template.replace(`{{${key}}}`, data[key]);
    }
    return template;
}
