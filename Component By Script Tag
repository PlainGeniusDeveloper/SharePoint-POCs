"Accordion": (content) => {
        const accordionItems = createAccordionItems(content.tabAccordionList); // Using the updated accordion method
        return getTemplate('accordion-template', {
            titleName: content.titleName,  // Bind title for the accordion
            accordionItems: accordionItems // Bind the dynamically created accordion items
        });
    },
