.container {
    display: flex;
    gap: 20px;
}

.column {
    width: 50%;
    border: 1px solid #ccc;
    padding: 10px;
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    height: 300px; /* Set a fixed height for demonstration, adjust as needed */
}

ul {
    list-style-type: none;
    padding: 0;
    min-height: 100px;
    flex-grow: 1; /* Allow the list to expand and fill space between the header and button */
    margin: 0; /* Remove margin for better layout control */
}

li {
    margin: 10px 0;
    padding: 5px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #f9f9f9;
    cursor: move;
}

.actions button {
    margin-left: 5px;
}

button {
    align-self: flex-end; /* Ensure button stays at the bottom of the column */
    margin-top: 10px; /* Optional spacing above the button */
}
